import { PlaceholderPage } from '../../components/PlaceholderPage'

export function CalendarPage() {
  return (
    <PlaceholderPage
      title="Calendar"
      subtitle="Read-only task calendar (month/week views)."
      hint="Milestone 6 will implement the read-only calendar UI."
    />
  )
}
