import { PlaceholderPage } from '../../components/PlaceholderPage'

export function NewJobPage() {
  return (
    <PlaceholderPage
      title="Job Controller / Create Job"
      subtitle="Select vehicle, shops, tasks, bays, users, and dependencies."
      hint="Milestone 5 will implement the full job creation wizard UI."
    />
  )
}
